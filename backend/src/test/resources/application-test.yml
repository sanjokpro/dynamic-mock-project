spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/dynamicmock-test
    redis:
      host: localhost
      port: 6379

graalvm:
  script:
    timeout-seconds: 5
    max-memory-mb: 128
    allow-host-access: true

logging:
  level:
    com.dynamicmock: DEBUG
    org.springframework: WARN

