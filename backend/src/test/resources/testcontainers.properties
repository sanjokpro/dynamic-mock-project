# Testcontainers configuration
# Skip docker-machine detection to avoid PATH parsing issues on Windows
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
checks.disable=true

